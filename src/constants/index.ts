// Delay with which the text on the title page types
export const typingDelayInMs = 150;
// Delay at which the "I like to code" text comes in
export const fadeDelayInMs = 1000;
// Repetition delay for the code pill
export const pillFadeDelay = 2500;
